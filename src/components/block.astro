---
import { transformBlock, type BlockSchema } from "@/schemas/block"

import { Block as ReactBlock } from "@/components/block"

type Props = BlockSchema

const props = await transformBlock(Astro.props)
---

<ReactBlock client:load {...props}>
  <Fragment set:html={Astro.props.content} />
</ReactBlock>
