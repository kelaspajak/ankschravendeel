---
import Block from "@/blocks/hero-4"
import type { Hero4Schema } from "@/schemas/blocks/hero-4"

type Props = Hero4Schema

const { content, ...props } = Astro.props
---

<Block {...props}>
  <Fragment set:html={content} />
</Block>
